version: '3.8'

services:
  ocr-benchmark:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./samples:/app/samples
      - ./results:/app/results
    command: --input /app/samples --output /app/results --language ko
